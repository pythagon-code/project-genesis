[build-system]
requires = [ "poetry-core>=2.0.0" ]
build-backend = "poetry.core.masonry.api"

[project]
name = "project_genesis"
version = "0.1.0"
description = "Will add later."
authors = [
    { name = "Abhay Pokhriyal", email = "abhaypokh89@gmail.com" }
]
license = "Apache-2.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [

]

[project.urls]
repository = ""